name: Build iOS and Android Caches

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *" # Daily at 6 AM UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-ios:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        name: Setup pnpm

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Generate Prisma client
        run: pnpm prisma generate

      - name: Build TypeScript
        run: pnpm build

      - name: Build iOS cache
        run: ./bin/jfp-app-cache-builder --target ios --verbose
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: Zip iOS database
        run: |
          cd assets/ios && zip ../../ios-cache.zip cache.realm

      - name: Upload iOS cache
        uses: actions/upload-artifact@v4
        with:
          name: ios-cache
          path: ios-cache.zip
          retention-days: 5

  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        name: Setup pnpm

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Generate Prisma client
        run: pnpm prisma generate

      - name: Build TypeScript
        run: pnpm build

      - name: Build Android cache
        run: ./bin/jfp-app-cache-builder --target android --verbose
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"
          DATABASE_URL: "file:${{ github.workspace }}/assets/android/cache.db?connection_limit=1&pool_timeout=60&query_timeout=60"

      - name: Zip Android database
        run: |
          cd assets/android && zip ../../android-cache.zip cache.db

      - name: Upload Android cache
        uses: actions/upload-artifact@v4
        with:
          name: android-cache
          path: android-cache.zip
          retention-days: 5

  deploy-pages:
    needs: [build-ios, build-android]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        name: Setup pnpm

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Download iOS artifact
        uses: actions/download-artifact@v4
        with:
          name: ios-cache
          path: ./public/data/ios/

      - name: Download Android artifact
        uses: actions/download-artifact@v4
        with:
          name: android-cache
          path: ./public/data/android/

      - name: Extract database files
        run: |
          # Extract iOS cache
          unzip -o ./public/data/ios/ios-cache.zip -d ./public/data/ios/

          # Extract Android cache  
          unzip -o ./public/data/android/android-cache.zip -d ./public/data/android/

      - name: Export database data
        run: pnpm export:dbs

      - name: Build Next.js site
        run: pnpm build:site
        env:
          NEXT_PUBLIC_BUILD_DATE: ${{ github.event.head_commit.timestamp || github.run_number }}
          NEXT_PUBLIC_BUILD_NUMBER: ${{ github.run_number }}
          NEXT_PUBLIC_COMMIT_SHA: ${{ github.sha }}

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
